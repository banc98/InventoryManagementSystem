@page
@model InvApp.Pages.Items.IndexModel
@{
    ViewData["Title"] = "Items";
}
<h2 class="mt-3">Items</h2>

<div class="d-flex justify-content-between align-items-center my-3">
  <form method="get" class="d-flex" role="search">
    <input class="form-control me-2" type="search" name="q" placeholder="Search…" disabled>
  </form>
  <a asp-page="Create" class="btn btn-primary">New Item</a>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>SKU</th>
      <th>Name</th>
      <th>Category</th>
      <th class="text-end">On Hand</th>
      <th class="text-end">Reorder</th>
      <th>Status</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  @foreach (var r in Model.Rows)
  {
    <tr>
      <td>@r.Sku</td>
      <td>@r.Name</td>
      <td>@r.Category</td>
      <td class="text-end">@r.OnHand</td>
      <td class="text-end">@r.ReorderLevel</td>
      <td>
        @if (r.IsActive) { <span class="badge bg-success">Active</span> }
        else { <span class="badge bg-secondary">Inactive</span> }
      </td>
      <td class="text-end">
        <a asp-page="Edit" asp-route-id="@r.Id" class="btn btn-sm btn-outline-secondary">Edit</a>
      </td>
    </tr>
  }
  </tbody>
</table>